<%= @build.name %><br>
<%= @build.runes %><br>
<%= @build.items %><br>
<%= @build.description %><br>

<%= link_to "Edit Build", edit_build_path(@build) %><br>

<%= link_to "Delete Build", @build, method: :delete %><br>
